<Page
    x:Name="RootPage"
    x:Class="ToolArchMilestone.InProgressPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:core="using:ToolArchMilestone.Core.Models"
    mc:Ignorable="d">

    <Grid Padding="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Archiviazioni in Corso" Style="{StaticResource TitleTextBlockStyle}" Margin="0,0,0,20"/>

        <!-- Search Bar -->
        <TextBox Grid.Row="1"
                 PlaceholderText="Cerca per ID Job, ID Lavoro, Camera..."
                 Text="{x:Bind ViewModel.SearchText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                 Margin="0,0,0,10"/>

        <ListView Grid.Row="2" ItemsSource="{x:Bind ViewModel.Jobs, Mode=OneWay}" SelectionMode="None">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="core:ArchivingJob">
                    <Grid Margin="0,10" Padding="10" Background="{ThemeResource SystemControlBackgroundChromeMediumLowBrush}" CornerRadius="4">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <!-- Job Info -->
                        <StackPanel Orientation="Horizontal" Spacing="10">
                            <TextBlock Text="{x:Bind Id}" FontWeight="Bold" Foreground="{ThemeResource SystemControlForegroundAccentBrush}"/>
                            <TextBlock Text="{x:Bind CameraName}" FontWeight="Bold"/>
                            <TextBlock Text="{x:Bind Status}" Foreground="LightGray"/>
                        </StackPanel>

                        <!-- Controls -->
                        <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="5">
                             <Button Content="Log/Dettagli">
                                 <Button.Flyout>
                                     <Flyout>
                                         <StackPanel Width="300" MaxHeight="400" Spacing="5">
                                             <TextBlock Text="Dettagli" Style="{StaticResource SubtitleTextBlockStyle}"/>
                                             <TextBlock Text="{x:Bind WorkId}" FontWeight="Bold"/>
                                             <TextBlock Text="{x:Bind CriminalProceeding}"/>
                                             <TextBlock Text="{x:Bind Magistrate}"/>
                                             <TextBlock Text="{x:Bind RitSpec}"/>
                                             <TextBlock Text="{x:Bind Target}"/>

                                             <TextBlock Text="Log" Style="{StaticResource SubtitleTextBlockStyle}" Margin="0,10,0,0"/>
                                             <ListView ItemsSource="{x:Bind Logs}">
                                                 <ListView.ItemTemplate>
                                                     <DataTemplate>
                                                         <TextBlock Text="{Binding Message}" FontSize="10" TextWrapping="Wrap"/>
                                                     </DataTemplate>
                                                 </ListView.ItemTemplate>
                                             </ListView>
                                         </StackPanel>
                                     </Flyout>
                                 </Button.Flyout>
                             </Button>
                             <Button Content="Stop" Command="{Binding ElementName=RootPage, Path=ViewModel.StopJobCommand}" CommandParameter="{x:Bind Id}"/>
                             <Button Content="Up" Command="{Binding ElementName=RootPage, Path=ViewModel.MoveUpCommand}" CommandParameter="{x:Bind Id}"/>
                             <Button Content="Down" Command="{Binding ElementName=RootPage, Path=ViewModel.MoveDownCommand}" CommandParameter="{x:Bind Id}"/>
                        </StackPanel>

                        <!-- Progress -->
                        <ProgressBar Grid.Row="1" Grid.ColumnSpan="2" Value="{x:Bind Progress, Mode=OneWay}" Margin="0,10"/>

                        <!-- Details -->
                        <TextBlock Grid.Row="2" Grid.ColumnSpan="2" Text="{x:Bind ErrorMessage, Mode=OneWay}" Foreground="Red" TextWrapping="Wrap"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
