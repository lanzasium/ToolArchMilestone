<Page
    x:Name="RootPage"
    x:Class="ToolArchMilestone.TerminatePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:core="using:ToolArchMilestone.Core.Models"
    mc:Ignorable="d">

    <Grid Padding="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Archiviazioni Terminate" Style="{StaticResource TitleTextBlockStyle}" Margin="0,0,0,20"/>

        <!-- Search Bar -->
        <TextBox Grid.Row="1"
                 PlaceholderText="Cerca per ID Job, ID Lavoro, Camera, Target..."
                 Text="{x:Bind ViewModel.SearchText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                 Margin="0,0,0,10"/>

        <ListView Grid.Row="2" ItemsSource="{x:Bind ViewModel.Jobs, Mode=OneWay}" SelectionMode="None">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="core:ArchivingJob">
                    <Grid Margin="0,10" Padding="10" Background="{ThemeResource SystemControlBackgroundChromeMediumLowBrush}" CornerRadius="4">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <!-- Job Info -->
                        <StackPanel Orientation="Vertical" Spacing="5">
                            <StackPanel Orientation="Horizontal" Spacing="10">
                                <TextBlock Text="{x:Bind Id}" FontWeight="Bold" Foreground="{ThemeResource SystemControlForegroundAccentBrush}"/>
                                <TextBlock Text="{x:Bind WorkId}" FontWeight="Bold"/>
                                <TextBlock Text="{x:Bind CameraName}"/>
                            </StackPanel>

                            <StackPanel Orientation="Horizontal" Spacing="10">
                                <TextBlock Text="{x:Bind Status}" Foreground="LightGray"/>
                                <TextBlock Text="{x:Bind StartTime}" Foreground="Gray" FontSize="12"/>
                                <TextBlock Text="->" Foreground="Gray" FontSize="12"/>
                                <TextBlock Text="{x:Bind EndTime}" Foreground="Gray" FontSize="12"/>
                            </StackPanel>

                            <TextBlock Text="{x:Bind Target}" FontStyle="Italic" FontSize="12"/>
                            <TextBlock Text="{x:Bind ExportPath}" FontSize="10" Foreground="Gray"/>
                        </StackPanel>

                        <!-- Log Button -->
                        <Button Grid.Column="1" Content="Log/Dettagli" Margin="10,0" VerticalAlignment="Center">
                             <Button.Flyout>
                                 <Flyout>
                                     <StackPanel Width="300" MaxHeight="400" Spacing="5">
                                         <TextBlock Text="Dettagli" Style="{StaticResource SubtitleTextBlockStyle}"/>
                                         <TextBlock Text="{x:Bind WorkId}" FontWeight="Bold"/>
                                         <TextBlock Text="{x:Bind CriminalProceeding}"/>
                                         <TextBlock Text="{x:Bind Magistrate}"/>
                                         <TextBlock Text="{x:Bind RitSpec}"/>
                                         <TextBlock Text="{x:Bind Target}"/>
                                         <TextBlock Text="{x:Bind GeneratedFilePath}" FontStyle="Italic" Foreground="Gray" TextWrapping="Wrap"/>

                                         <TextBlock Text="Log" Style="{StaticResource SubtitleTextBlockStyle}" Margin="0,10,0,0"/>
                                         <ListView ItemsSource="{x:Bind Logs}">
                                             <ListView.ItemTemplate>
                                                 <DataTemplate>
                                                     <TextBlock Text="{Binding Message}" FontSize="10" TextWrapping="Wrap"/>
                                                 </DataTemplate>
                                             </ListView.ItemTemplate>
                                         </ListView>
                                     </StackPanel>
                                 </Flyout>
                             </Button.Flyout>
                        </Button>

                        <!-- Controls -->
                        <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="5">
                            <Button Content="Elimina" VerticalAlignment="Center"
                                    Command="{Binding ElementName=RootPage, Path=ViewModel.DeleteJobCommand}"
                                    CommandParameter="{x:Bind Id}"/>

                            <Button Content="Elimina + File" VerticalAlignment="Center"
                                    Command="{Binding ElementName=RootPage, Path=ViewModel.DeleteJobWithFileCommand}"
                                    CommandParameter="{x:Bind Id}"
                                    Background="DarkRed" Foreground="White"/>
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
