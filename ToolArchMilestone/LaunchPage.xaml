<Page
    x:Class="ToolArchMilestone.LaunchPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:converters="using:ToolArchMilestone.Converters"
    mc:Ignorable="d">

    <Page.Resources>
        <converters:BooleanToVisibilityConverter x:Key="BoolToVis"/>
        <converters:DateTimeToDateTimeOffsetConverter x:Key="DateToOffset"/>
    </Page.Resources>

    <ScrollViewer>
        <Grid Padding="20" RowSpacing="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/> <!-- Title -->
                <RowDefinition Height="*"/>    <!-- Main Content -->
                <RowDefinition Height="Auto"/> <!-- Footer Buttons -->
            </Grid.RowDefinitions>

            <!-- Header -->
            <!-- <TextBlock Text="Nuova Archiviazione" Style="{StaticResource TitleTextBlockStyle}" /> -->
            <!-- We can skip title to save space if needed, or keep it small -->

            <!-- Main Content: Split 2 Columns -->
            <Grid Grid.Row="1" ColumnSpacing="20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="3*"/> <!-- Left: Formato & Sorgente -->
                    <ColumnDefinition Width="2*"/> <!-- Right: Configurazione -->
                </Grid.ColumnDefinitions>

                <!-- LEFT COLUMN -->
                <StackPanel Spacing="20">
                    <!-- FORMATO -->
                    <Border BorderBrush="{ThemeResource SystemControlForegroundAccentBrush}" BorderThickness="2,0,0,0" Padding="10,0,0,0">
                        <StackPanel Spacing="10">
                            <TextBlock Text="FORMATO" FontWeight="Bold" Foreground="{ThemeResource SystemControlForegroundAccentBrush}" FontSize="16"/>
                            <StackPanel Orientation="Horizontal" Spacing="20">
                                <TextBlock Text="Tipologia archiviazione" VerticalAlignment="Center" Foreground="Gray" FontSize="12"/>
                                <RadioButton Content="Legale" IsChecked="True" GroupName="Type" />
                                <RadioButton Content="Digitale" GroupName="Type" />
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" Spacing="20">
                                <TextBlock Text="Modalità di lancio" VerticalAlignment="Center" Foreground="Gray" FontSize="12"/>
                                <RadioButton Content="Da server" IsChecked="{x:Bind ViewModel.IsServer, Mode=OneWay}" GroupName="Source" />
                                <RadioButton Content="Da archivio" IsChecked="{x:Bind ViewModel.IsArchive, Mode=OneWay}" GroupName="Source" />
                            </StackPanel>
                        </StackPanel>
                    </Border>

                    <!-- SORGENTE -->
                    <Border BorderBrush="Green" BorderThickness="2,0,0,0" Padding="10,0,0,0" Visibility="{x:Bind ViewModel.IsServer, Mode=OneWay, Converter={StaticResource BoolToVis}}">
                        <StackPanel Spacing="10">
                            <TextBlock Text="SORGENTE" FontWeight="Bold" Foreground="Green" FontSize="16"/>

                            <Grid ColumnDefinitions="*, Auto, Auto" ColumnSpacing="10">
                                <TextBox Header="Indirizzo Server" Text="{x:Bind ViewModel.ServerAddress, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" PlaceholderText="http://localhost"/>
                                <Button Content="Connetti" VerticalAlignment="Bottom" Grid.Column="1"/>
                                <Button Content="📌" VerticalAlignment="Bottom" Grid.Column="2"/>
                            </Grid>

                            <Grid ColumnDefinitions="*, Auto" ColumnSpacing="10">
                                <TextBox Header="Telecamera" Text="{x:Bind ViewModel.CameraName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" IsReadOnly="True" PlaceholderText="Seleziona una o più telecamere..."/>
                                <Button Content="Seleziona" VerticalAlignment="Bottom" Grid.Column="1"/>
                            </Grid>

                            <TextBlock Text="Intervallo data/ora" FontWeight="SemiBold" Margin="0,5,0,0" FontSize="12"/>
                            <Grid ColumnDefinitions="*, Auto, *, Auto" ColumnSpacing="10">
                                <!-- Using CalendarDatePicker as requested for better typing support -->
                                <CalendarDatePicker Header="Inizio" Date="{x:Bind ViewModel.StartTime, Mode=TwoWay, Converter={StaticResource DateToOffset}}" HorizontalAlignment="Stretch"/>
                                <!-- Placeholder for calendar icon if needed, but CalendarDatePicker has one -->

                                <CalendarDatePicker Header="Fine" Grid.Column="2" Date="{x:Bind ViewModel.EndTime, Mode=TwoWay, Converter={StaticResource DateToOffset}}" HorizontalAlignment="Stretch"/>
                            </Grid>

                            <!-- Drop Area for TXT -->
                            <Grid Margin="0,10,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <Border x:Name="DropArea" BorderBrush="Gray" BorderThickness="1" CornerRadius="4" Padding="10" AllowDrop="True" DragOver="DropArea_DragOver" Drop="DropArea_Drop">
                                    <TextBlock Text="Rilascia o clicca qui per inserire più intervalli" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="Gray"/>
                                </Border>

                                <Button Grid.Column="1" Content="Controlla" Margin="5,0"/>
                                <Button Grid.Column="2" Content="X" Background="Red" Foreground="White"/>
                            </Grid>

                            <CheckBox Content="Crea un'archiviazione separata per ogni intervallo" IsChecked="{x:Bind ViewModel.SeparateArchive, Mode=TwoWay}"/>
                        </StackPanel>
                    </Border>
                </StackPanel>


                <!-- RIGHT COLUMN -->
                <StackPanel Grid.Column="1" Spacing="20">
                    <!-- CONFIGURAZIONE -->
                    <Border BorderBrush="Orange" BorderThickness="2,0,0,0" Padding="10,0,0,0">
                        <StackPanel Spacing="10">
                            <TextBlock Text="CONFIGURAZIONE" FontWeight="Bold" Foreground="Orange" FontSize="16"/>

                            <TextBox Header="Procedimento Penale" Text="{x:Bind ViewModel.CriminalProceeding, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" PlaceholderText="es: 12345/2025/N"/>
                            <TextBox Header="Magistrato" Text="{x:Bind ViewModel.Magistrate, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" PlaceholderText="Inserisci il magistrato di riferimento..."/>
                            <TextBox Header="RIT/SPEC" Text="{x:Bind ViewModel.RitSpec, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" PlaceholderText="Inserisci RIT o SPEC (es: RIT 1234/2025)"/>
                            <TextBox Header="ID Lavoro" Text="{x:Bind ViewModel.WorkId, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" PlaceholderText="Inserisci ID lavoro..."/>
                            <TextBox Header="Target" Text="{x:Bind ViewModel.Target, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" PlaceholderText="Inserisci nome target..."/>

                            <Grid ColumnDefinitions="*, Auto, Auto" ColumnSpacing="5">
                                <TextBox Header="Password" Text="{x:Bind ViewModel.Password, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" PlaceholderText="Inserisci 8 caratteri..."/>
                                <Button Content="Genera" VerticalAlignment="Bottom" Grid.Column="1" Command="{x:Bind ViewModel.GeneratePasswordCommand}" Background="Orange" Foreground="Black"/>
                                <Button Content="📌" VerticalAlignment="Bottom" Grid.Column="2"/>
                            </Grid>

                            <Grid ColumnDefinitions="*, Auto, Auto" ColumnSpacing="5">
                                <TextBox Header="Cartella di esportazione" Text="{x:Bind ViewModel.ExportPath, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" PlaceholderText="Seleziona o incolla il percorso..."/>
                                <Button Content="Sfoglia" VerticalAlignment="Bottom" Grid.Column="1" Background="Orange" Foreground="Black"/>
                                <Button Content="📌" VerticalAlignment="Bottom" Grid.Column="2"/>
                            </Grid>

                            <TextBlock Text="Dimensione stimata: ---" Foreground="Gray" FontSize="12"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Grid>

            <!-- FOOTER -->
            <StackPanel Grid.Row="2" Spacing="10">
                <Button Content="AVVIA ARCHIVIAZIONE" HorizontalAlignment="Stretch" Height="40" Background="{ThemeResource SystemControlForegroundAccentBrush}" Foreground="White" FontWeight="Bold" Command="{x:Bind ViewModel.LaunchCommand}"/>
                <Button Content="RECUPERA ULTIMO LANCIO" HorizontalAlignment="Stretch" Background="Transparent" Foreground="Gray" BorderBrush="Gray" BorderThickness="1"/>
            </StackPanel>
        </Grid>
    </ScrollViewer>
</Page>
