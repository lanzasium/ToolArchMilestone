<Page
    x:Class="ToolArchMilestone.LaunchPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:converters="using:ToolArchMilestone.Converters"
    mc:Ignorable="d">

    <Page.Resources>
        <converters:BooleanToVisibilityConverter x:Key="BoolToVis"/>
        <converters:DateTimeToDateTimeOffsetConverter x:Key="DateToOffset"/>

        <!-- Text-Only Toggle Style for Header -->
        <Style x:Key="HeaderToggleStyle" TargetType="RadioButton">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="Gray"/>
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="4,0"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Grid Background="Transparent">
                            <VisualStateManager.VisualStateGroups>
                                <VisualStateGroup x:Name="CommonStates">
                                    <VisualState x:Name="Normal"/>
                                    <VisualState x:Name="PointerOver">
                                        <Storyboard>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="ContentPresenter" Storyboard.TargetProperty="Foreground">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="White"/>
                                            </ObjectAnimationUsingKeyFrames>
                                        </Storyboard>
                                    </VisualState>
                                    <VisualState x:Name="Checked">
                                        <Storyboard>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="ContentPresenter" Storyboard.TargetProperty="Foreground">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="White"/>
                                            </ObjectAnimationUsingKeyFrames>
                                        </Storyboard>
                                    </VisualState>
                                </VisualStateGroup>
                            </VisualStateManager.VisualStateGroups>
                            <ContentPresenter x:Name="ContentPresenter" Content="{TemplateBinding Content}" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="{TemplateBinding Foreground}"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <Grid Padding="12" Background="{StaticResource WindowBackground}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- Header -->
            <RowDefinition Height="*"/>    <!-- Content -->
        </Grid.RowDefinitions>

        <!-- Header Row: Title + Inline Toggles -->
        <StackPanel Orientation="Horizontal" Margin="0,0,0,12" VerticalAlignment="Center">
            <TextBlock Text="NUOVA ARCHIVIAZIONE" FontSize="20" FontWeight="Bold" Foreground="White" Margin="0,0,8,0"/>

            <TextBlock Text="-" FontSize="20" Foreground="Gray" Margin="0,0,8,0"/>

            <!-- Format Toggles -->
            <RadioButton Content="LEGALE" IsChecked="True" GroupName="Type" Style="{StaticResource HeaderToggleStyle}"/>
            <TextBlock Text="/" FontSize="20" Foreground="Gray"/>
            <RadioButton Content="DIGITALE" GroupName="Type" Style="{StaticResource HeaderToggleStyle}"/>

            <TextBlock Text="-" FontSize="20" Foreground="Gray" Margin="8,0,8,0"/>

            <!-- Source Toggles -->
            <RadioButton Content="DA SERVER" IsChecked="{x:Bind ViewModel.IsServer, Mode=OneWay}" GroupName="Source" Style="{StaticResource HeaderToggleStyle}"/>
            <TextBlock Text="/" FontSize="20" Foreground="Gray"/>
            <RadioButton Content="DA ARCHIVIO" IsChecked="{x:Bind ViewModel.IsArchive, Mode=OneWay}" GroupName="Source" Style="{StaticResource HeaderToggleStyle}"/>
        </StackPanel>

        <ScrollViewer Grid.Row="1">
            <StackPanel Spacing="12">

                <!-- TOP SECTION: SORGENTE -->
                <Border Style="{StaticResource MilestoneCardStyle}">
                    <Grid>
                        <Rectangle Height="2" VerticalAlignment="Top" Fill="{StaticResource MilestoneBlue}" Margin="0,-12,0,0"/>
                        <StackPanel Spacing="8">

                            <!-- Server & Camera (Row 1) - Visible only if Server -->
                            <Grid ColumnSpacing="12" Visibility="{x:Bind ViewModel.IsServer, Mode=OneWay, Converter={StaticResource BoolToVis}}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <Grid Grid.Column="0" ColumnDefinitions="*, Auto" ColumnSpacing="4">
                                    <TextBox Header="Indirizzo Server" Text="{x:Bind ViewModel.ServerAddress, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" PlaceholderText="http://..."/>
                                    <Button Content="Connetti" VerticalAlignment="Bottom" Grid.Column="1"/>
                                </Grid>

                                <Grid Grid.Column="1" ColumnDefinitions="*, Auto" ColumnSpacing="4">
                                    <TextBox Header="Telecamera" Text="{x:Bind ViewModel.CameraName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" IsReadOnly="True" PlaceholderText="Seleziona..."/>
                                    <Button Content="Seleziona" VerticalAlignment="Bottom" Grid.Column="1"/>
                                </Grid>
                            </Grid>

                            <!-- Interval & Import (Row 2 - Single Line) -->
                            <Grid ColumnSpacing="12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/> <!-- Start -->
                                    <ColumnDefinition Width="Auto"/> <!-- End -->
                                    <ColumnDefinition Width="*"/>    <!-- Drop -->
                                    <ColumnDefinition Width="Auto"/> <!-- Checkbox -->
                                </Grid.ColumnDefinitions>

                                <!-- Start Date/Time Group -->
                                <StackPanel Grid.Column="0" Spacing="2">
                                    <TextBlock Text="Inizio" Foreground="{StaticResource TextSecondary}" FontSize="12"/>
                                    <Border BorderBrush="{StaticResource TextSecondary}" BorderThickness="1" CornerRadius="4" Padding="2">
                                        <StackPanel Orientation="Horizontal" Spacing="2">
                                            <CalendarDatePicker Date="{x:Bind ViewModel.StartTime, Mode=TwoWay, Converter={StaticResource DateToOffset}}" IsEnabled="{x:Bind ViewModel.IsManualIntervalEnabled, Mode=OneWay}" Width="140" BorderThickness="0"/>
                                            <TimePicker SelectedTime="{x:Bind ViewModel.StartTimeTime, Mode=TwoWay}" IsEnabled="{x:Bind ViewModel.IsManualIntervalEnabled, Mode=OneWay}" ClockIdentifier="24HourClock" BorderThickness="0"/>
                                        </StackPanel>
                                    </Border>
                                </StackPanel>

                                <!-- End Date/Time Group -->
                                <StackPanel Grid.Column="1" Spacing="2">
                                    <TextBlock Text="Fine" Foreground="{StaticResource TextSecondary}" FontSize="12"/>
                                    <Border BorderBrush="{StaticResource TextSecondary}" BorderThickness="1" CornerRadius="4" Padding="2">
                                        <StackPanel Orientation="Horizontal" Spacing="2">
                                            <CalendarDatePicker Date="{x:Bind ViewModel.EndTime, Mode=TwoWay, Converter={StaticResource DateToOffset}}" IsEnabled="{x:Bind ViewModel.IsManualIntervalEnabled, Mode=OneWay}" Width="140" BorderThickness="0"/>
                                            <TimePicker SelectedTime="{x:Bind ViewModel.EndTimeTime, Mode=TwoWay}" IsEnabled="{x:Bind ViewModel.IsManualIntervalEnabled, Mode=OneWay}" ClockIdentifier="24HourClock" BorderThickness="0"/>
                                        </StackPanel>
                                    </Border>
                                </StackPanel>

                                <!-- Drop Area -->
                                <Grid Grid.Column="2" VerticalAlignment="Bottom" Margin="0,0,0,0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <Border x:Name="DropArea" Height="36" BorderBrush="{StaticResource TextSecondary}" BorderThickness="1,1,1,1" CornerRadius="4" AllowDrop="True" DragOver="DropArea_DragOver" Drop="DropArea_Drop" Background="{ThemeResource SystemControlBackgroundChromeMediumLowBrush}" VerticalAlignment="Bottom">
                                        <Grid>
                                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="8" Visibility="{x:Bind ViewModel.IsManualIntervalEnabled, Mode=OneWay, Converter={StaticResource BoolToVis}}">
                                                <SymbolIcon Symbol="Import" Foreground="{StaticResource MilestoneBlue}" RenderTransformOrigin="0.5,0.5">
                                                    <SymbolIcon.RenderTransform>
                                                        <ScaleTransform ScaleX="0.8" ScaleY="0.8"/>
                                                    </SymbolIcon.RenderTransform>
                                                </SymbolIcon>
                                                <TextBlock Text="Trascina o Clicca" Foreground="{StaticResource TextSecondary}" FontSize="11" VerticalAlignment="Center"/>
                                            </StackPanel>

                                            <TextBlock Text="{x:Bind ViewModel.ImportedFileName, Mode=OneWay}" Foreground="#00FF00" FontSize="11" VerticalAlignment="Center" HorizontalAlignment="Center" FontWeight="Bold" Visibility="{x:Bind ViewModel.IsFileImported, Mode=OneWay, Converter={StaticResource BoolToVis}}"/>

                                            <Button HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Opacity="0" Command="{x:Bind ViewModel.ImportIntervalsFromFileCommand}"/>
                                        </Grid>
                                    </Border>

                                    <Button Grid.Column="1" Content="Verifica" Margin="4,0,0,0" VerticalAlignment="Stretch" FontSize="11" Padding="4" Visibility="{x:Bind ViewModel.IsFileImported, Mode=OneWay, Converter={StaticResource BoolToVis}}">
                                        <Button.Flyout>
                                            <Flyout>
                                                <TextBlock Text="{x:Bind ViewModel.IntervalsText, Mode=OneWay}" MaxWidth="300" MaxHeight="400" TextWrapping="Wrap"/>
                                            </Flyout>
                                        </Button.Flyout>
                                    </Button>

                                    <Button Grid.Column="2" Content="X" Margin="4,0,0,0" VerticalAlignment="Stretch" Background="Transparent" Foreground="#E00000" Padding="4" Command="{x:Bind ViewModel.RemoveImportedFileCommand}" Visibility="{x:Bind ViewModel.IsFileImported, Mode=OneWay, Converter={StaticResource BoolToVis}}"/>
                                </Grid>

                                <!-- Checkbox -->
                                <CheckBox Grid.Column="3" Content="Separa archiviazioni" IsChecked="{x:Bind ViewModel.SeparateArchive, Mode=TwoWay}" FontSize="11" VerticalAlignment="Bottom" Margin="0,0,0,6"/>
                            </Grid>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- BOTTOM SECTION: DATI PROCEDURA -->
                <Border Style="{StaticResource MilestoneCardStyle}">
                    <Grid>
                        <Rectangle Height="2" VerticalAlignment="Top" Fill="{StaticResource MilestoneYellow}" Margin="0,-12,0,0"/>
                        <StackPanel Spacing="12">
                            <TextBlock Text="DATI PROCEDURA" Style="{StaticResource MilestoneHeaderStyle}" Foreground="{StaticResource MilestoneYellow}"/>

                            <!-- Row 1: Proceeding & Magistrate -->
                            <Grid ColumnSpacing="12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBox Grid.Column="0" Header="Procedimento Penale" Text="{x:Bind ViewModel.CriminalProceeding, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" PlaceholderText="es. 12345/2023"/>
                                <TextBox Grid.Column="1" Header="Magistrato" Text="{x:Bind ViewModel.Magistrate, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                            </Grid>

                            <!-- Row 2: RIT/SPEC & Work ID -->
                            <Grid ColumnSpacing="12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBox Grid.Column="0" Header="RIT/SPEC" Text="{x:Bind ViewModel.RitSpec, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                                <TextBox Grid.Column="1" Header="ID Lavoro (RCS)" Text="{x:Bind ViewModel.WorkId, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                            </Grid>

                            <!-- Row 3: Target & Password -->
                            <Grid ColumnSpacing="12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBox Grid.Column="0" Header="Target" Text="{x:Bind ViewModel.Target, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                                <Grid Grid.Column="1" ColumnDefinitions="*, Auto, Auto" ColumnSpacing="4">
                                    <PasswordBox Header="Password" Password="{x:Bind ViewModel.Password, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                                    <Button Content="Genera" VerticalAlignment="Bottom" Grid.Column="1" Command="{x:Bind ViewModel.GeneratePasswordCommand}"/>
                                    <Button Content="📌" VerticalAlignment="Bottom" Grid.Column="2" Background="Transparent" Command="{x:Bind ViewModel.PinFieldCommand}" CommandParameter="Password"/>
                                </Grid>
                            </Grid>

                            <!-- Row 4: Export Path -->
                            <Grid ColumnDefinitions="*, Auto, Auto" ColumnSpacing="4">
                                <TextBox Header="Cartella Esportazione" Text="{x:Bind ViewModel.ExportPath, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                                <Button Content="Sfoglia" VerticalAlignment="Bottom" Grid.Column="1"/>
                                <Button Content="📌" VerticalAlignment="Bottom" Grid.Column="2" Background="Transparent" Command="{x:Bind ViewModel.PinFieldCommand}" CommandParameter="ExportPath"/>
                            </Grid>

                        </StackPanel>
                    </Grid>
                </Border>

                <!-- FOOTER ACTION -->
                <Button Command="{x:Bind ViewModel.LaunchCommand}"
                        Background="{StaticResource MilestoneBlue}" Foreground="White"
                        HorizontalAlignment="Stretch" Height="48" CornerRadius="8" FontSize="16" FontWeight="Bold">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <SymbolIcon Symbol="Play"/>
                        <TextBlock Text="AVVIA ARCHIVIAZIONE"/>
                    </StackPanel>
                </Button>

            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
