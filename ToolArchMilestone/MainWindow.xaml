<Window
    x:Class="ToolArchMilestone.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:converters="using:ToolArchMilestone.Converters"
    mc:Ignorable="d"
    Title="Tool Arch Milestone">

    <Window.SystemBackdrop>
        <MicaBackdrop Kind="Base"/>
    </Window.SystemBackdrop>

    <Grid x:Name="RootGrid" Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" DataContext="{x:Bind ViewModel}">
        <NavigationView x:Name="NavView" 
                        PaneDisplayMode="Top" 
                        SelectionChanged="NavView_SelectionChanged" 
                        Loaded="NavView_Loaded">
            <NavigationView.MenuItems>
                <NavigationViewItem Tag="Launch" Content="LANCIO" Icon="Play" />
                <NavigationViewItem Tag="InProgress" Content="IN CORSO" Icon="Sync" />
                <NavigationViewItem Tag="History" Content="TERMINATE" Icon="Library" />
            </NavigationView.MenuItems>

            <Frame x:Name="ContentFrame" />
        </NavigationView>

        <!-- Status Bar Overlay (Top Right of Menu Area) -->
        <StackPanel Orientation="Horizontal" Spacing="8" Margin="0,8,24,0" VerticalAlignment="Top" HorizontalAlignment="Right"
                    Visibility="{Binding IsConnected, Mode=OneWay, Converter={StaticResource BoolToVis}}">
            <TextBlock Text="Connesso a:" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}" VerticalAlignment="Center"/>
            <TextBlock Text="{x:Bind ViewModel.ConnectedServerName, Mode=OneWay}" FontSize="12" FontWeight="SemiBold" VerticalAlignment="Center"/>
            <FontIcon Glyph="&#xE73E;" FontSize="12" Foreground="#4CAF50" VerticalAlignment="Center"/>
        </StackPanel>
    </Grid>
</Window>
