<Window
    x:Class="ToolArchMilestone.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:converters="using:ToolArchMilestone.Converters"
    mc:Ignorable="d"
    Title="Tool Arch Milestone">

    <Window.SystemBackdrop>
        <MicaBackdrop Kind="Base"/>
    </Window.SystemBackdrop>

    <Grid x:Name="RootGrid" Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" DataContext="{x:Bind ViewModel}">
        <NavigationView x:Name="NavView" 
                        PaneDisplayMode="Top" 
                        SelectionChanged="NavView_SelectionChanged" 
                        Loaded="NavView_Loaded">
            <NavigationView.MenuItems>
                <NavigationViewItem Tag="Launch" Content="LANCIO" Icon="Play" />
                <NavigationViewItem Tag="InProgress" Content="IN CORSO" Icon="Sync" />
                <NavigationViewItem Tag="History" Content="TERMINATE" Icon="Library" />
            </NavigationView.MenuItems>

            <Frame x:Name="ContentFrame" />
        </NavigationView>

        <!-- Status Bar Overlay (Top Right of Menu Area) -->
        <Border Margin="0,0,60,0" VerticalAlignment="Center" HorizontalAlignment="Right"
                Background="#252525" BorderBrush="#333" BorderThickness="1" CornerRadius="8" Padding="12,6"
                Visibility="{Binding IsConnected, Mode=OneWay, Converter={StaticResource BoolToVis}}">
            <StackPanel Orientation="Horizontal" Spacing="8">
                <FontIcon Glyph="&#xE701;" FontSize="14" Foreground="#4CAF50" VerticalAlignment="Center"/>
                <TextBlock Text="{Binding ConnectedServerName, Mode=OneWay}" FontSize="14" FontWeight="SemiBold" VerticalAlignment="Center" Foreground="White"/>
                <TextBlock Text="•" FontSize="14" Foreground="{ThemeResource TextFillColorSecondaryBrush}" VerticalAlignment="Center"/>
                <TextBlock Text="Connesso" FontSize="14" Foreground="#4CAF50" VerticalAlignment="Center"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
